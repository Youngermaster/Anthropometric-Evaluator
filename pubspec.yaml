name: anthropometric_evaluator
description: This is an anthropometric evaluator.

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  curved_navigation_bar: ^0.3.2
  material_design_icons_flutter: ^3.4.4995
  google_fonts: ^0.3.9
  cupertino_icons: ^0.1.2
  decimal: ^0.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
